const User = require('./User');
const Payment = require('./Payment');

// İlişkileri kurma işlemi modellerin içerisinde yapıldı
// Tüm modelleri export ediyoruz
 
module.exports = {
  User,
  Payment
}; 